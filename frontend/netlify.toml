[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# 生产环境API地址（部署时需要修改为实际后端地址）
VITE_LOCAL_API_BASE = "https://monopodially-invadable-leona.ngrok-free.dev"